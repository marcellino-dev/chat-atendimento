spring:
  application:
    name: chat-atendimento

  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3307/chat_atendimento?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: chatuser
    password: chatpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
  servlet:
    context-path: /api

app:
  jwt:
    secret: MUDE_ESSA_CHAVE_EM_PRODUCAO_USE_256_BITS_MINIMO
    expiration: 86400000
    refresh-expiration: 604800000
  websocket:
    allowed-origins: http://localhost:3000
  whatsapp:
    graph-api-url: https://graph.facebook.com
    api-version: v21.0
    phone-number-id: ${WHATSAPP_PHONE_NUMBER_ID}
    access-token: ${WHATSAPP_TOKEN}
    verify-token: ${WHATSAPP_VERIFY_TOKEN:meuTokenSecretoWebhook123}

logging:
  level:
    com.empresa.chat: DEBUG
    org.springframework.security: WARN